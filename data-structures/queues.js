const queue = [];
queue.push("Harry");
queue.push("Ron");
queue.push("Hermione");

console.log(queue)
queue.shift();
console.log(queue);

/*
FIFO (first-in first-out), primeiro elemento a ser inserido, é o primeiro a ser retirado.
*/
